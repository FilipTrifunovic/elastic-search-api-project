namespace elastic_search_api.Infrastructure.Persistence.Configurations.Mappings
{
    public static class AuditableEntityMapping
    {
        //public static PropertiesDescriptor<T> AddAuditableEntityMapping<T>(this PropertiesDescriptor<T> c)
        //{
            //return c.Keyword(n => n.Name(n => n.Id))
            //            .Date(d => d.Name(n => n.CreatedAt))
            //            .Text(t => t.Name(n => n.CreatedBy))
            //            .Date(d => d.Name(n => n.LastModified))
            //            .Text(t => t.Name(n => n.LastModifiedBy))
            //            .Boolean(b => b.Name(n => n.IsActive));      
        //}
    }
}
